# Local database containers for multi-database testing
# Usage: docker-compose up -d
#
# Both databases use the same credentials for simplicity:
# - Username: labelgraph
# - Password: labelgraph
# - Database: laravel_label_graph
#
# Ports are offset to avoid conflicts with other local databases:
# - MySQL: 13306 (instead of 3306)
# - PostgreSQL: 15432 (instead of 5432)

services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: labelgraph
      MYSQL_DATABASE: laravel_label_graph
      MYSQL_USER: labelgraph
      MYSQL_PASSWORD: labelgraph
    ports:
      - "13306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 3

  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: labelgraph
      POSTGRES_PASSWORD: labelgraph
      POSTGRES_DB: laravel_label_graph
    ports:
      - "15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U labelgraph"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  mysql_data:
  postgres_data:
